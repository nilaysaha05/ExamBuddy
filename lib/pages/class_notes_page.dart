import 'package:flutter/material.dart';

class ClassNotes extends StatelessWidget {
  const ClassNotes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Class Notes Page"),
      ),
    );
  }
}
